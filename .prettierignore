# Dependencies and build output
node_modules/
dist/
coverage/

# Generated lockfile
package-lock.json

# Hand-maintained specification documents.
# These must stay byte-stable: reformatting them would produce huge unrelated diffs,
# and the golden fixture is a rendering contract, not prose to be reflowed.
ROADMAP.md
tests/fixtures/

# The author's content library.
# These are the documents MDHoriZon reads, written by hand over years. A formatter must never touch
# them: `prettier --write` rewrites `*emphasis*` to `_emphasis_` and `*` bullets to `-`, which is a
# change to 100+ files nobody asked for, and this project's job is to read that text, not to restyle it.
# Byte-stability here is a promise to the author, not a preference.
content/

# Native projects generated by Capacitor (Phase 14+)
android/
ios/

# Local reference checkouts (gitignored). Prettier already honours .gitignore, so this is
# belt-and-braces: it keeps third-party style out of `format:check` even if the ignore entry
# changes. See docs/references.md and ADR 0002.
8vo/
